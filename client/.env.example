# Client — copy to .env for split FE/BE deploys
# Leave empty for same-origin (Vite dev proxy + express serving dist/).
# Example for separate API host:
# VITE_API_URL=https://api.example.com
VITE_API_URL=
